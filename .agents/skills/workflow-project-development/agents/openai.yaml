interface:
  display_name: "Project Development Workflow"
  short_description: "Start, resume, navigate, and reconcile SDLC workflow state."
  default_prompt: "Inspect this project's workflow state, explain where we are, and guide me through the next valid stage using the existing specialist skills."

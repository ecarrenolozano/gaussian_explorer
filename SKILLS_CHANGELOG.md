# Skill Consistency Fixes

- Normalized all skill frontmatter to `name` and `description`.
- Corrected mismatched skill names for implementation planning and testing.
- Standardized project-context and raw-ideas filenames.
- Added canonical readiness, issue-preview, testing-results, validation, and deployment artifact paths.
- Standardized testing references on `00_testing_strategy.md` and `02_testing_results.md`.
- Corrected downstream references from `h-implementation` to `h-implementation-planning`.
- Renamed the malformed duplicate checklist filename.
- Added explicit output locations and approval-gate rules.
- Added required `agents/openai.yaml` metadata to every skill.
- Added `WORKFLOW_CONVENTIONS.md` as the canonical cross-skill artifact map.
- Closed a malformed JSON code fence in the project-context skill.

# SDLC documentation

This directory contains the authoritative artifacts produced by the AI-assisted workflow. Numbered folders reflect the expected development sequence; iteration is allowed, but downstream work must use the latest approved upstream artifacts.
